import { ModuleenablePipe } from './moduleenable.pipe';

describe('ModuleenablePipe', () => {
  it('create an instance', () => {
    const pipe = new ModuleenablePipe();
    expect(pipe).toBeTruthy();
  });
});
